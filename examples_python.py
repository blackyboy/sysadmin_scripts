#!/usr/bin/env python
#for Loop
for a in [1,2]:
	for b in ['a', 'b']:
		print a, b

#if/else condition

#!/usr/bin/env python
import os
if os.path.isdir("tmp"):
	print "tmp is directory"
else:
	print "/tmp is not a directory"
